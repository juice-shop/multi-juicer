apiVersion: v1
kind: Service
metadata:
  name: multi-juicer-metrics
  labels:
    {{ include "multi-juicer.labels" . | nindent 4 }}
    type: metrics
spec:
  type: ClusterIP
  selector:
    {{ include "multi-juicer.selectorLabels" . | nindent 4 }}
  ports:
    - port: 80
      name: metrics
